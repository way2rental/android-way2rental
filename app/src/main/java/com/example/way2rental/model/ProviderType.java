package com.example.way2rental.model;

public enum ProviderType {
    DB,S3
    // Add other types if your backend expects them, e.g., LOCAL, S3
    // For now, only DB as specified in your FileUploadRequest default.
}